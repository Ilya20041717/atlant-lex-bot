BOT_TOKEN=replace_with_bot_token
DB_URL=sqlite+aiosqlite:///./data/app.db
AGENCY_NAME=Агентство по банкротству физических лиц
DEFAULT_ORG_ID=1
# ID админов в Telegram (кому присылать заявки из опросника), через запятую
ADMIN_TG_IDS=654078115
# Через сколько секунд удалять сообщения бота (0 = не удалять)
DELETE_MESSAGE_AFTER_SECONDS=60
# Ключ OpenAI для кнопки «Спросить ИИ» (оставьте пустым — ИИ будет отключён)
OPENAI_API_KEY=
# Модель (gpt-4o-mini дешевле, gpt-4o умнее)
OPENAI_MODEL=gpt-4o-mini
# Макс. длина ответа в токенах (100–4096)
OPENAI_MAX_TOKENS=500
# Ассистент OpenAI (опционально): если задан — бот использует его вместо обычного чата.
# Создайте ассистента на platform.openai.com → Assistants → Create, загрузите файлы (ГК РФ, инфо о компании), скопируйте ID (asst_...).
# OPENAI_ASSISTANT_ID=asst_xxxxxxxxxxxx
OPENAI_ASSISTANT_ID=
# Доп. инструкции для ИИ (используются только без ассистента): тон, темы.
# AI_SYSTEM_PROMPT_EXTRA=Фокус на банкротстве физлиц. Упоминай этапы процедуры.
AI_SYSTEM_PROMPT_EXTRA=

# DeepSeek (OpenAI-совместимый API)
DEEPSEEK_API_KEY=
# Обычно: https://api.deepseek.com/v1
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# Например: deepseek-chat (или другой доступный в аккаунте)
DEEPSEEK_MODEL=deepseek-chat

# Стартовое изображение (одно из двух):
# - START_IMAGE_FILE_ID: самый надёжный вариант (Telegram file_id)
# - START_IMAGE_PATH: путь к файлу на этом компьютере (в проекте: static/start_logo.png)
START_IMAGE_FILE_ID=
START_IMAGE_PATH=

# ИИ провайдер:
# - auto: OpenAI если задан ключ, иначе DeepSeek если задан ключ, иначе Ollama если задан URL, иначе выключено
# - openai: только OpenAI
# - deepseek: только DeepSeek
# - ollama: только локальный Ollama
# - none: выключить ИИ
AI_PROVIDER=auto
OLLAMA_BASE_URL=http://localhost:11434
# Пример: llama3.1:8b-instruct / qwen2.5:7b-instruct / mistral:7b-instruct
OLLAMA_MODEL=

# ПАУ (Витрина данных). Если не заполнено — бот работает в демо-режиме.
# Рабочий сервер обычно: https://datamart.rsit.ru/api.php
PAU_BASE_URL=
PAU_LOGIN=
PAU_PASSWORD=
# customer|manager|partner|viewer
PAU_CATEGORY=customer
# Суд по умолчанию для регистрации заявления (можно поменять под ваш процесс)
PAU_DEFAULT_COURT_NAME=Арбитражный суд города Москвы
